#include <stdio.h>
int main()
{	
    
        int num[4];
        int i;
        printf("key in the four elements of the array:\n");
          
        int sum=num[1]+num[2]+num[3]+num[4];
        
        int mean=sum/6;
        {
            {
            	 scanf("%d\n",&num[i]);
            }
        }
            printf("The sum all of the elements is:%d\n",sum);
            printf("The mean of all the elements is:%d\n",mean);
    return 0;
}
